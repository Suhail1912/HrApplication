import { EmployeeAddressDetails } from './employee-address-details';

describe('EmployeeAddressDetails', () => {
  it('should create an instance', () => {
    expect(new EmployeeAddressDetails()).toBeTruthy();
  });
});
