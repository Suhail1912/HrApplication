import { EmployeeOfficeDetails } from './employee-office-details';

describe('EmployeeOfficeDetails', () => {
  it('should create an instance', () => {
    expect(new EmployeeOfficeDetails()).toBeTruthy();
  });
});
